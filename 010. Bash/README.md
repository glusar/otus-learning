### Bash
### Задание
Написать скрипт для CRON, который раз в час формирует отчёт и отправляет его на заданную почту.  
Отчёт должен содержать:  
- IP-адреса с наибольшим числом запросов (с момента последнего запуска);
- Запрашиваемые URL с наибольшим числом запросов (с момента последнего запуска);
- Ошибки веб-сервера/приложения (с момента последнего запуска);
- HTTP-коды ответов с указанием их количества (с момента последнего запуска).

Скрипт должен предотвращать одновременный запуск нескольких копий, до его завершения.  
В письме должен быть прописан обрабатываемый временной диапазон.  

### Скрипт
Скрипт находится в файле `script.sh`  
К нему прилагается конфигурационный файл `script.conf` - в нем указана переменная `OLD_TIME_LABEL` с начальной меткой времени, после запуска скрипта, это значение будет автоматически меняться на новое.  
Пример лог-файла `access-4560-644067.log` - желательно положить в /var/log, в скрипте есть переменная `LOG_FILE_PATH`, в которой задан этот путь  

Сделаем скрипт исполняемым:  
```bash
chmod +x /usr/local/bin/script.sh
```

Настроим cron для запуска скрипта каждый час:  
```bash
crontab -e
```

Пишем:  
```
0 * * * * /usr/local/bin/script.sh
```
